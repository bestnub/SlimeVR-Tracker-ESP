#ifndef MULTIPLEXER
#define MULTIPLEXER

namespace Multiplexer
{
    /*!
     *  @brief Setup Multiplexer
     */
    void setup();

    /*!
     *  @brief Set Multiplexer Channel
     */
    void setTCAChannel(int i);
}
#endif